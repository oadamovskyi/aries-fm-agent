import type { Handler, HandlerInboundMessage } from '../../../../../../agent/Handler';
import type { MessagePickupService } from '../MessagePickupService';
import { BatchPickupMessage } from '../messages';
export declare class BatchPickupHandler implements Handler {
    private messagePickupService;
    supportedMessages: (typeof BatchPickupMessage)[];
    constructor(messagePickupService: MessagePickupService);
    handle(messageContext: HandlerInboundMessage<BatchPickupHandler>): Promise<import("../../../../../../types").OutboundMessage<import("../messages").BatchMessage>>;
}
